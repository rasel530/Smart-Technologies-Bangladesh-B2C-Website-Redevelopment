# Docker Environment Configuration
# This file contains environment-specific settings for Docker deployment
# Use this configuration when running the application in Docker containers

# Environment
NODE_ENV=production
PORT=3000

# Database Configuration (Docker)
DATABASE_URL=postgresql://smart_dev:smart_dev_password_2024@postgres:5432/smart_ecommerce_dev
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=smart_dev
DB_PASSWORD=smart_dev_password_2024
DB_NAME=smart_ecommerce_dev

# Redis Configuration (Docker)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis_smarttech_2024
REDIS_URL=redis://:redis_smarttech_2024@redis:6379
REDIS_TTL=3600

# Docker Environment Detection
IS_DOCKER=true
DOCKER_ENV=true

# JWT Configuration
JWT_SECRET=smarttech-super-secret-jwt-key-change-in-production-2024
JWT_EXPIRES_IN=15m

# Security Configuration
BCRYPT_ROUNDS=12

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Service URLs (Docker)
ELASTICSEARCH_NODE=http://elasticsearch:9200
OLLAMA_HOST=http://ollama:11434

# Testing Configuration (Disabled in Production)
DISABLE_EMAIL_VERIFICATION=false
DISABLE_PHONE_VERIFICATION=false
TESTING_MODE=false

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH="./uploads"

# Payment Gateway Configuration
BKASH_API_KEY=bkash_demo_api_key_replace_with_production
BKASH_API_SECRET=bkash_demo_api_secret_replace_with_production
NAGAD_API_KEY=nagad_demo_api_key_replace_with_production
NAGAD_API_SECRET=nagad_demo_api_secret_replace_with_production
ROCKET_API_KEY=rocket_demo_api_key_replace_with_production
ROCKET_API_SECRET=rocket_demo_api_secret_replace_with_production