# ============================================
# SMART TECHNOLOGIES BANGLADESH - FRONTEND ENVIRONMENT VARIABLES
# ============================================
# 
# This file contains all environment variables required for frontend application.
# Copy this file to .env and fill in actual values.
# 
# IMPORTANT: Never commit .env file to version control!
# ============================================

# ============================================
# NEXTAUTH.JS CONFIGURATION
# ============================================
# NextAuth.js secret (MUST be at least 32 characters)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-at-least-32-characters-long

# NextAuth.js URL (defaults to localhost:3000 in development)
NEXTAUTH_URL=http://localhost:3000

# ============================================
# BACKEND API CONFIGURATION
# ============================================
# Backend API base URL
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:3001/api

# ============================================
# GOOGLE OAUTH CONFIGURATION
# ============================================
# Get credentials from: https://console.cloud.google.com
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google
#    - http://localhost:3000/auth/callback/google
#    - https://yourdomain.com/api/auth/callback/google (production)

NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# FACEBOOK OAUTH CONFIGURATION
# ============================================
# Get credentials from: https://developers.facebook.com
# 1. Create a new app
# 2. Add Facebook Login product
# 3. Configure OAuth redirect URIs:
#    - http://localhost:3000/api/auth/callback/facebook
#    - http://localhost:3000/auth/callback/facebook
#    - https://yourdomain.com/api/auth/callback/facebook (production)

NEXT_PUBLIC_FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret

# ============================================
# APPLICATION CONFIGURATION
# ============================================
# Application name
NEXT_PUBLIC_APP_NAME=Smart Technologies Bangladesh

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# ENVIRONMENT CONFIGURATION
# ============================================
# Node environment (development, production, test)
NODE_ENV=development

# ============================================
# FEATURE FLAGS
# ============================================
# Enable/disable features
NEXT_PUBLIC_ENABLE_SOCIAL_AUTH=true
NEXT_PUBLIC_ENABLE_EMAIL_AUTH=true
NEXT_PUBLIC_ENABLE_PHONE_AUTH=true

# ============================================
# BILINGUAL SUPPORT
# ============================================
# Default language (en, bn)
NEXT_PUBLIC_DEFAULT_LANGUAGE=en

# ============================================
# SECURITY CONFIGURATION
# ============================================
# Session timeout in seconds (default: 30 days = 2592000)
NEXT_PUBLIC_SESSION_TIMEOUT=2592000

# ============================================
# PRODUCTION CONFIGURATION
# ============================================
# Production Environment
# NODE_ENV=production
# NEXTAUTH_URL=https://smarttechnologies.bd
# NEXT_PUBLIC_BACKEND_API_URL=https://api.smarttechnologies.bd/api
# NEXT_PUBLIC_APP_URL=https://smarttechnologies.bd
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-production-google-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=your-production-google-client-secret
# NEXT_PUBLIC_FACEBOOK_APP_ID=your-production-facebook-app-id
# FACEBOOK_APP_SECRET=your-production-facebook-app-secret
# NEXTAUTH_URL=https://smarttechnologies.bd

# ============================================
# NOTES AND GUIDELINES
# ============================================
# 
# 1. NextAuth.js Configuration:
#    - NEXTAUTH_SECRET is required for JWT token signing
#    - Generate a strong secret using: openssl rand -base64 32
#    - NEXTAUTH_URL should match your application's base URL
# 
# 2. OAuth Configuration:
#    - Google OAuth: https://console.cloud.google.com
#      * Create OAuth 2.0 Client ID
#      * Add redirect URIs for both development and production
#      * Enable Google+ API
#    - Facebook OAuth: https://developers.facebook.com
#      * Create a new app
#      * Add Facebook Login product
#      * Configure redirect URIs
#      * Set app to public or review for public use
# 
# 3. Backend API Configuration:
#    - Ensure backend is running on the specified URL
#    - Backend must have CORS configured to allow frontend origin
#    - Backend OAuth endpoints must be accessible
# 
# 4. Security:
#    - Never commit .env file to version control
#    - Use different secrets for development and production
#    - Rotate secrets periodically
#    - Use strong, randomly generated secrets
# 
# 5. Testing:
#    - Use development OAuth credentials for local testing
#    - Test with different browsers
#    - Verify redirect URIs match exactly
# 
# ============================================
